1) Почему-то переодически 46 строка кода возвращала пустой список.
   Иногда отрабатывало несколько запросов подряд без проблем, иногда нужно было выдержать
   существенную паузу.
   Причем response каждый раз получал без ошибки, но внутри блока лежал пустой список.

2) Не понял, как преобразовать елемент etree.Element в строку.
   Скажем, я получаю в тэге <a> @href. Как этот href преобразовать к строке.
   Пробовал через tostring(), ругается.